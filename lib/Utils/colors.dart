import 'package:flutter/material.dart';

// Its a good practice to name the constants by starting their names by `k`
// Made it static so that we can have its access on class level


class ColorsConstants {
  static const kMobileBackgroundColor = Color.fromRGBO(0, 0, 0, 1);
  static const kWebBackgroundColor = Color.fromRGBO(18, 18, 18, 1);
  static const kMobileSearchColor = Color.fromRGBO(38, 38, 38, 1);
  static const kBlueColor = Color.fromRGBO(0, 149, 246, 1);
  static const kPrimaryColor = Colors.white;
  static const kSecondaryColor = Colors.grey;
}
